name: Temporary - show delivery customers

on:
  push:
    branches:
      - feat/motoboys-clientes-entregues

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout branch
        uses: actions/checkout@v4
        with:
          ref: feat/motoboys-clientes-entregues
          fetch-depth: 0

      - name: Add delivered customer names to courier cards
        shell: bash
        run: |
          python3 - <<'PY'
          from pathlib import Path

          path = Path("src/routes/_authenticated/motoboys.tsx")
          text = path.read_text()

          if "Clientes entregues" in text:
              raise SystemExit(0)

          section = '>Resultado no período</div>'
          section_pos = text.find(section)
          if section_pos < 0:
              raise SystemExit("Resultado no período section not found")

          needle = '                      <div className="mt-4 grid grid-cols-2 gap-2 text-sm">'
          insert_pos = text.find(needle, section_pos)
          if insert_pos < 0:
              raise SystemExit("Insertion point not found")

          block = '''                      {(history?.deliveredOrders.length ?? 0) > 0 && (
                        <div className="mt-4 rounded-xl border border-primary/25 bg-primary/5 p-3">
                          <div className="mb-2 flex items-center gap-2 text-xs font-semibold uppercase tracking-wide text-muted-foreground">
                            <CheckCircle2 className="h-3.5 w-3.5 text-primary" /> Clientes entregues
                          </div>
                          <div className="space-y-2">
                            {history?.deliveredOrders.slice(0, 3).map((delivery: any) => (
                              <div key={delivery.id} className="flex items-center justify-between gap-3 rounded-lg bg-background/60 px-2.5 py-2 text-xs">
                                <div className="min-w-0">
                                  <div className="truncate font-semibold text-foreground">{delivery.order?.customer || "Cliente não informado"}</div>
                                  <div className="text-[10px] text-muted-foreground">Pedido #{String(delivery.order?.id || delivery.order_id || "").slice(0, 8)}</div>
                                </div>
                                <div className="shrink-0 text-[10px] text-muted-foreground">
                                  {delivery.completed_at
                                    ? new Date(delivery.completed_at).toLocaleTimeString("pt-BR", { hour: "2-digit", minute: "2-digit" })
                                    : ""}
                                </div>
                              </div>
                            ))}
                            {(history?.deliveredOrders.length ?? 0) > 3 && (
                              <div className="text-[11px] text-muted-foreground">+ {(history?.deliveredOrders.length ?? 0) - 3} outra(s) entrega(s). Veja todas em “Ver carga e histórico”.</div>
                            )}
                          </div>
                        </div>
                      )}

'''

          path.write_text(text[:insert_pos] + block + text[insert_pos:])
          PY

          git diff --check
          grep -q "Clientes entregues" src/routes/_authenticated/motoboys.tsx

      - name: Commit patch
        shell: bash
        run: |
          if git diff --quiet; then
            echo "No changes to commit"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/routes/_authenticated/motoboys.tsx
          git commit -m "feat: mostrar clientes entregues nos cards de motoboys"
          git push origin HEAD:feat/motoboys-clientes-entregues
